@using UI.Models.Badges
@model IEnumerable<UI.Models.Nemeses.NemesisChangeViewModel>
<div id="nemesisChangeVote" class="fl-right" style="margin-top: 21px; margin-bottom: 10.5px;"></div>
<div class="fl-clear"></div>
<div class="flip-scroll">
	<table class="table table-hover table-striped" id="recentNemesisChangesTable">
		<thead>
			<tr>
				<th>Player</th>
				<th>
					New Nemesis <i class="fa fa-question-circle" data-container="body" data-toggle="popover" data-placement="right"
						 data-content="@NemesisBadgeViewModel.POPOVER_TEXT"></i>
				</th>
				<th>Loss %</th>
			</tr>
		</thead>
        @{ var position = 1; }
		@foreach (UI.Models.Nemeses.NemesisChangeViewModel nemesisChange in @Model)
		{
			<tr data-position="@position">
                @{ position++; }
				<td>@Html.ActionLink(nemesisChange.MinionPlayerName, "Details", "Player", new { id = nemesisChange.MinionPlayerId }, new { })</td>
				<td>@Html.ActionLink(nemesisChange.NemesisPlayerName, "Details", "Player", new { id = nemesisChange.NemesisPlayerId }, new { })</td>
				<td>@nemesisChange.LossPercentageVersusNemesis&nbsp;%</td>
			</tr>
		}
	</table>
</div>