@model BusinessLogic.Models.PlayedGame

@{
    ViewBag.Title = "Delete Played Game";
    Layout = MVC.Shared.Views._BaseLayout;
}

<h2 class="page-header">Delete</h2>

<h3>Are you sure you want to delete this?</h3>
@*//TODO this should really be the same view as the details view.*@
<div>
    <h4>PlayedGame</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.GameDefinition.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.GameDefinition.Name)
        </dd>

        <dt>
            Number of Players
        </dt>

        <dd>
            @Html.DisplayFor(model => model.NumberOfPlayers)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> 
        </div>
    }
</div>
